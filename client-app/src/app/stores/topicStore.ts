import { CreateTopic } from "../models/topic";

export default class TopicStore {

    createTopic = async (values: CreateTopic) => {
        try {
            
        } catch (e: any) {
            
        }
    }
}